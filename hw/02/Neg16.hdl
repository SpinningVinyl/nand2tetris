/**
 * 16-bit negation:
 * out = -in (in 2's complement)
 */

CHIP Neg16 {
    IN in[16];
    OUT out[16];

    PARTS:
   // Put you code here:
   Not16(in = in, out = NotIn);
   Inc16(a = NotIn, out = out);
}
